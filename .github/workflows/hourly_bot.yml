name: Plant Biotech Bot - Hourly Posts

on:
  schedule:
    # Запускаем каждый час (в 0 минут каждого часа)
    - cron: '0 * * * *'
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Клонируем репозиторий
      uses: actions/checkout@v3
      
    - name: 🐍 Устанавливаем Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Устанавливаем зависимости
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 📂 Восстанавливаем состояние (если есть)
      uses: actions/download-artifact@v3
      with:
        name: bot-state
        path: ./
      continue-on-error: true  # Продолжаем если артефакта нет
      
    - name: 🤖 Запускаем бота
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      run: python main.py
      
    - name: 💾 Сохраняем состояние для следующего запуска
      uses: actions/upload-artifact@v3
      with:
        name: bot-state
        path: |
          posted_links.txt
          source_rotation.json
          last_post_time.txt
          bot.log
        retention-days: 7
